<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
	<!--
	<thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

	-->
    <% @products.each do |product| %>
		<tr class="<%= cycle('list_line_odd', 'list_line_even')%>">
			<td> <%=image_tag(product.image, class: 'list_image') %></td>
			<td class="list_description">
				<dl>
					<dt><%= product.name %></dt>
					<dd><%= product.description %> <br><br> <%= number_to_currency(product.price )%></td> </dd>
				</dl>
			</td>

        <td class="list_actions"><%= link_to 'Show', product %>
        <%= link_to 'Edit', edit_product_path(product) %>
        <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
